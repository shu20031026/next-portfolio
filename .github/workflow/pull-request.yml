name: client-test

<<<<<<< HEAD
on:
  pull_request:
    branches:
      - "**"
    paths:
      - "**"
=======
on: pull_request
>>>>>>> 7b5ac469c4e2f44bcdd7067fa401982032209bd0

jobs:
  typecheck:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [14]

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@master

      - name: Setup node.js 📦
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node }}
          cache: yarn

      - name: Install dependencies 👨🏻‍💻
        run: yarn

      - name: Type check 👀
        run: yarn typecheck

      - name: Build check
        run: yarn build